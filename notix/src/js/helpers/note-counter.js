export default function noteCounter() {
    const colors = [{color: "", qty: 0}];

    for(const item of vars.tempStore) {
        if(item.itemType === "note") {
            
        }
    }

    console.log("COLORS: ", colors);
}